[project]
name = "orchestator-pb"
version = "0.1.0"
authors = [
    { name = "Chaos", email = "gabysolotorevsky100@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
