service Manager {
  rpc Prompt(PromptRequest) returns (stream PromptResponse)
}

enum Agents {
  CHARLATAN = 0;
  SOÑADOR = 1;
}

message PromptRequest {
  string text = 1;
  Agents agent = 2;
}

message PromptResponse {
  string token = 1;
  // Agents agent = 2; maybe for later
}
