[X] Make it so the server sends to the client a stream of tokens when getting the prompt
[] FIX: The session singleton is not working, and creates a new session every call
[] Make a queue for the petitions, to only answer 1 at a time (or interrupt the current one)
