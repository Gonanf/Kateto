{
  "name": "trainer",
  "private": true,
  "scripts": {
    "test": "uv run pytest apps/trainer/tests/",
    "train": "uv run python -m trainer.train_llm",
    "train-bert": "uv run python -m trainer.train_bert",
    "convert": "uv run python -m trainer.convert",
    "deploy": "uv run python -m trainer.deploy"
  }
}
